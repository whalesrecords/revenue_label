{
  "name": "csv-merge",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "cd client && npm install --no-package-lock && cd ../server && npm install --no-package-lock",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm install --no-package-lock",
    "build": "npm run install:all && npm run build:client && npm run build:server"
  },
  "dependencies": {
    "@netlify/functions": "^3.1.8"
  }
}
