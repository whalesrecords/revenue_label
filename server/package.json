{
  "name": "csv-merge-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "csv-stringify": "^6.5.2",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.2",
    "serverless-http": "^3.2.0",
    "csv-parse": "^5.5.3",
    "@netlify/functions": "^3.1.8"
  },
  "scripts": {
    "start": "node index.js",
    "prebuild": "rm -rf functions-build && mkdir -p functions-build",
    "build": "cp -r functions/* functions-build/"
  }
} 